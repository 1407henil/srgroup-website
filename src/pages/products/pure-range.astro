---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Container from "../../components/ui/Container.astro";

const glycols = ["Mono Ethylene Glycol (MEG)", "Diethylene Glycol (DEG)"];
const chlorinated = ["SRSolv"];
---

<BaseLayout
  title="Pure Range | S R Group"
  description="Pure grade glycols and chlorinated solvents from S R Industries and S R Plast."
>

<section class="range-intro">
  <Container>
    <span class="eyebrow">S R INDUSTRIES &amp; S R PLAST</span>
    <h1>Pure Range</h1>
    <p class="lead">
      Pure grade glycols and chlorinated solvents, supplied to meet a range
      of industrial specifications.
    </p>
  </Container>
</section>

<section class="range-body">
  <Container>
    <div class="category-block">
      <div class="category-group">
        <span class="group-label">Glycols</span>
        <ul>
          {glycols.map((item) => <li>{item}</li>)}
        </ul>
      </div>

      <div class="category-group">
        <span class="group-label">Chlorinated</span>
        <ul>
          {chlorinated.map((item) => <li>{item}</li>)}
        </ul>
      </div>
    </div>

    <!-- Detailed specifications for each product to be added later -->

    <div class="group-link">
      <a href={`${import.meta.env.BASE_URL}companies/industries`}>
        Learn more about S R Industries &amp; S R Plast
      </a>
    </div>
  </Container>
</section>

</BaseLayout>

<style>

.eyebrow{
  display:inline-block;
  margin-bottom:1.25rem;
  font-size:.95rem;
  font-weight:700;
  letter-spacing:.24em;
  text-transform:uppercase;
  color:var(--color-accent);
  border-left:3px solid var(--color-accent);
  padding-left:.75rem;
}

.lead{
  font-size:1.1rem;
  line-height:1.9;
  color:var(--color-text-light);
  max-width:60ch;
}

.range-intro{
  padding:7rem 0 4rem;
}

.range-intro h1{
  margin:0 0 1.5rem;
  font-family:var(--font-heading);
  font-size:clamp(2.6rem,5vw,4rem);
  line-height:1.05;
  color:var(--color-primary);
}

.range-body{
  padding:0 0 6rem;
}

.category-block{
  padding:2.5rem;
  background:var(--color-bg);
  border:1px solid var(--color-border);
  border-radius:var(--radius);
  max-width:560px;
}

.category-group{
  margin-bottom:1.5rem;
}

.category-group:last-child{
  margin-bottom:0;
}

.group-label{
  display:block;
  margin-bottom:.6rem;
  font-size:.7rem;
  font-weight:600;
  letter-spacing:.16em;
  text-transform:uppercase;
  color:var(--color-accent);
}

.category-group ul{
  margin:0;
  padding:0;
  list-style:none;
}

.category-group li{
  padding:.4rem 0;
  font-size:1rem;
  color:var(--color-text);
  border-bottom:1px solid var(--color-border);
}

.category-group li:last-child{
  border-bottom:none;
}

.group-link{
  margin-top:2.5rem;
}

.group-link a{
  font-size:.95rem;
  font-weight:600;
  color:var(--color-primary);
  text-decoration:none;
  border-bottom:1px solid var(--color-accent);
  padding-bottom:2px;
}

.group-link a:hover{
  color:var(--color-accent);
}

@media(max-width:600px){
  .range-intro{
    padding:5rem 0 3rem;
  }
}

</style>
